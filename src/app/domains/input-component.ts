import { InputBlockConfig } from "./config-objects"

export interface InputComponentData{
    titleText : string
    subText : string
    nextUrl : string
    createAccountLabel : string
    inputBlockConfigs: InputBlockConfig[]
}